
@model PagedList.IPagedList<Department_Store.Models.SanPham>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Home";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<header>
    <img src="~/Content/images/banner.jpg" width="100%" height="250px" />
</header>
<br />
<div class="danhmuc">
    @Html.Action("Danhmuc", "Danhmuc")
</div>
<br />
@*<div class="row">
        @foreach (var item in Model)
        {
            <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
                <div class="small-container">

                    <div class="col-3">
                        <div class="card" style="width: 20rem; height:550px">
                            <img class="card-img-top" src="@item.HinhSP" alt="Card image cap">
                            <div class="card-body">
                                <h5 class="card-title">@item.TenSP</h5>
                                <strong>Giá: </strong>@item.GiaSP
                                <br />
                                <strong>Số lượng tồn: </strong>@item.Slgton
                                <p>Ngày nhập: @item.NgayNhap</p>


                            </div>
                            <a class="btn btn-outline-success" onclick="addToCart(@item.MaSP)">Đặt Mua</a>
                        </div>
                    </div>
                    <br />
                </div>
            </div>
        }
    </div>*@


    <section>
        <div class="container py-5">
            <div class="row">
                @foreach (var item in Model)
                {
                    <div class="col-md-12 col-lg-4 mb-4 mb-lg-0">
                        <div class="card">
                            <div class="d-flex justify-content-between p-3">
                                <p ><strong>@item.TenSP</strong></p>
                            </div>
                            <img src="@item.HinhSP"
                                 class="card-img-top" alt="Laptop" />
                            <div class="card-body">
                                <div class="d-flex justify-content-between">
                                </div>
                                <h5 class="text-dark mb-0"><strong>Giá: </strong>@item.GiaSP VND</h5>
                                <strong>Số lượng tồn: </strong>@item.Slgton
                                <p><strong>Ngày nhập: </strong>Ngày nhập: @item.NgayNhap</p>
                            </div>
                        </div>
                    </div>
                    <br />
                }
                </div>
        </div>
    </section>
    <br />
    <div>
        <div>
            Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
        </div>
        <div class="MenuTrang ">
            @Html.PagedListPager(Model, page => Url.Action("Home", new { page = page }))
        </div>
    </div>
    <script type="text/javascript">
    function addToCart(id) {
        $.ajax({
            type: 'POST',
            url: "@Url.Action("ThemGioHang", "GioHang")",
            data: { id: id },
            success: function (result) {
                getGioHangPartial();
                if (result.success == true) {
                    {
                        alert(result.message);
                    }
                }
            }
            });
    }
    </script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script>
        $(document).ready(function () {
            var maxLength = 100;
            $(".show-read-more").each(function () {
                var myStr = $(this).text();
                if ($.trim(myStr).length > maxLength) {
                    var newStr = myStr.substring(0, maxLength);
                    var removedStr = myStr.substring(maxLength, $.trim(myStr).length);
                    $(this).empty().html(newStr);
                    $(this).append(' <a href="javascript:void(0);" class="read-more">đọc tiếp...</a>');
                    $(this).append('<span class="more-text">' + removedStr + '</span>');
                }
            });
            $(".read-more").click(function () {
                $(this).siblings(".more-text").contents().unwrap();
                $(this).remove();
            });
        });
    </script>

